<header class="header">
    <img class="logo" src="<%= config.admin.root %>/images/logo-min-transparentbg.png" />
    <span class="title">theJobListr</span>

    <div class="right">
        <div class="loggedin-user">
            Logged in as <b><%= auth.uname %></b>. <a class="link" href="<%= config.admin.root + '/logout' %>">Log Out</a>
        </div>
    </div>
</header>